import styled from "styled-components/macro";

export const GameStyled = styled.div`
  background: #17323A;
  border-radius: 1rem;
  display: flex;
  flex-direction: column;
  max-width: 25rem;
  width: 100%;
  position: relative;
  
  & > div:first-child {
    
  }
  
  & > div:nth-child(2) {
    
  }
`;