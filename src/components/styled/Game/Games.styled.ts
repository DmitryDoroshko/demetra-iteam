import styled from "styled-components/macro";

export const GamesStyled = styled.main`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(25rem, 1fr));
  justify-items: center;
  gap: 2rem;
  width: 100%;
  margin-bottom: 2rem;
`;